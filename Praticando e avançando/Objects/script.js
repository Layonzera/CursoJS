// Object

const person = {
    name: 'Adryann',
    age: 30,
    weight: 64.5,
    isAdmin: true
}

console.log(person.name) // para pegar um nome de objeto é só colocar um ".". 'Ex: person.age or person.weight'

console.log(`${person.name} tem ${person.age} anos.`) // usando a interpolação